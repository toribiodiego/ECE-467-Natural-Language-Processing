# Python Dependencies for GoEmotions Emotion Classification Project
#
# This file specifies all Python packages required for data analysis,
# model training, and visualization.
#
# Installation:
#   pip install -r requirements.txt
#
# Note: Versions are pinned for reproducibility. PyTorch version assumes
# CUDA 11.8 for GPU support. For CPU-only or different CUDA versions,
# see: https://pytorch.org/get-started/locally/

# Core ML/NLP Libraries
datasets==2.14.5           # HuggingFace datasets for loading GoEmotions
transformers==4.30.2       # HuggingFace transformers (BERT, RoBERTa, DistilBERT)
torch>=2.0.1               # PyTorch for model training (flexible version for Python 3.12+)
torchvision>=0.15.2        # PyTorch vision utilities
torchaudio>=2.0.2          # PyTorch audio (dependency)

# Scientific Computing
numpy==1.24.3              # Numerical computing
scipy==1.10.1              # Scientific computing
scikit-learn==1.3.0        # Machine learning utilities (metrics, preprocessing)

# Data Manipulation
pandas==2.0.3              # Data manipulation and analysis
tabulate==0.9.0            # Pretty-print tabular data

# Visualization
matplotlib==3.7.2          # Plotting library
seaborn==0.12.2            # Statistical data visualization
plotly==5.15.0             # Interactive visualizations (optional)

# Jupyter Notebook
jupyter==1.0.0             # Jupyter metapackage
notebook==6.5.4            # Classic Jupyter notebook
ipywidgets==8.0.7          # Interactive widgets for Jupyter
ipykernel==6.24.0          # IPython kernel for Jupyter

# Experiment Tracking (optional but recommended)
wandb==0.15.5              # Weights & Biases for experiment tracking

# Utilities
tqdm==4.65.0               # Progress bars
python-dotenv==1.0.0       # Load environment variables from .env
requests==2.31.0           # HTTP library

# Development Tools (optional)
black==23.7.0              # Code formatter
flake8==6.0.0              # Linter
pytest==7.4.0              # Testing framework

# Additional Dependencies
tokenizers==0.13.3         # Fast tokenizers for transformers
huggingface-hub==0.16.4    # HuggingFace Hub utilities
safetensors==0.3.1         # Safe tensor serialization
psutil==5.9.5              # System and process utilities
pyyaml==6.0.1              # YAML parser
pyarrow==12.0.1            # Apache Arrow (pinned for datasets compatibility)

# Note: If you encounter CUDA/GPU issues with PyTorch, you may need to
# reinstall with specific CUDA version:
#   pip install torch==2.0.1 torchvision==0.15.2 --index-url https://download.pytorch.org/whl/cu118
