# Environment Configuration for GoEmotions Project
#
# This file defines environment variables for consistent path references
# across all scripts and modules.
#
# Usage:
#   1. Copy this file to .env: cp .env.example .env
#   2. Modify paths if needed (defaults work for most cases)
#   3. Scripts will automatically load these variables
#
# Note: .env is gitignored to allow per-developer customization

# Virtual environment directory
# Location where Python virtualenv will be created by setup.sh
VENV_DIR=venv

# Artifacts directory
# Large binary files (model checkpoints, cached datasets)
# These files are gitignored due to size
ARTIFACTS_DIR=artifacts

# Output directory
# Generated analysis results (figures, stats, reports)
# These files are gitignored as they can be regenerated
OUTPUT_DIR=output

# Project root (optional - auto-detected by most scripts)
# PROJECT_ROOT=.

# Python version requirement (optional)
# PYTHON_VERSION=3.8
